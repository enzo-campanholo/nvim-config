# Neovim config
Linux and Mac:

    git clone https://github.com/enzo-campanholo/nvim-config "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim
    
Windows:

    git clone https://github.com/enzo-campanholo/nvim-config %userprofile%\AppData\Local\nvim\
